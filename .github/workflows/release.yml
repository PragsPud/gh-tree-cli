name: Build and Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            build_file: gh_tree
            release_file: gh_tree_linux_amd64
          - os: macos-latest
            build_file: gh_tree
            release_file: gh_tree_macos_arm64
          - os: windows-latest
            build_file: gh_tree.exe
            release_file: gh_tree_windows_x64.exe

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install curl nlohmann-json

      - name: Install Dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libcurl4-openssl-dev nlohmann-json3-dev

      - name: Setup MSYS2 & Dependencies (Windows)
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-toolchain
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-ninja
            mingw-w64-ucrt-x86_64-nlohmann-json
            mingw-w64-ucrt-x86_64-curl

      - name: Build (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          mkdir build && cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
          make

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        shell: msys2 {0}
        run: |
          mkdir build && cd build
          cmake -G Ninja -DCMAKE_BUILD_TYPE=Release ..
          ninja

      - name: Rename Binary for Release
        shell: bash
        run: mv build/${{ matrix.build_file }} build/${{ matrix.release_file }}

      - name: Upload Release
        uses: softprops/action-gh-release@v1
        with:
          files: build/${{ matrix.release_file }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
